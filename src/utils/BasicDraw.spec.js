import * as BasicDraw from './BasicDraw'

describe('(Utils) BasicDraw', () => {
  describe('drawLine', () => {
    it('Should be a function', () => {
      expect(BasicDraw.drawLine).to.be.a('function')
    })
  })
  describe('drawRect', () => {
    it('Should be a function', () => {
      expect(BasicDraw.drawRect).to.be.a('function')
    })
  })
  describe('setColor', () => {
    it('Should be a function', () => {
      expect(BasicDraw.setColor).to.be.a('function')
    })
  })
  describe('clear', () => {
    it('Should be a function', () => {
      expect(BasicDraw.clear).to.be.a('function')
    })
  })
  describe('drawImage', () => {
    it('Should be a function', () => {
      expect(BasicDraw.drawImage).to.be.a('function')
    })
  })
})
